@react.component
let make = () => {
  <div className="px-6 w-full">
    <div className="text-sm text-center w-full">
      {React.string("Clickk on the button below to start the game!")}
    </div>
  </div>
}