
; (library
;   (name readfile)
;   (modules readfile)
;   (libraries 
;   torch npy core_unix core_unix.sys_unix
;     )
    ; )


(executables
  (names example readfile)
  (libraries core torch npy core_unix core_unix.sys_unix))
